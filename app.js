App({
  globalData: {
    recent: ['git merge', 'git reset', 'npm init', 'git clone', 'ls -la', 'docker run', 'grep -r'],
    favorites: ['git commit', 'npm install', 'tar -czf', 'git push', 'ls -la']
  }
})
